org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler cannot reliably close its cache files on this
# Windows host (build fails with "Could not close incremental caches"), most
# likely due to on-access AV scanning of the build directory. Full compilation
# is a few seconds slower but does not intermittently fail.
kotlin.incremental=false
