name: mplayer
description: "Cross-platform video player and media server client"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.4.2
  go_router: ^17.4.0
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^11.0.0
  path: ^1.9.1
  path_provider: ^2.1.6
  window_manager: ^0.5.2
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7
  dio: ^5.11.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  collection: ^1.19.1
  # Pinned by flutter_localizations, which depends on exactly 0.20.2.
  intl: ^0.20.2
  freezed: 3.2.5
  file_selector: ^1.1.0
  screen_brightness: ^2.1.11
  xml: ^7.0.1
  flutter_localizations:
    sdk: flutter
  receive_sharing_intent: ^1.9.0
  dart_smb2: ^0.1.1
# media_kit_libs_android_video is a *transitive* dependency: media_kit_libs_video
# pulls it in. It is overridden here because the version on pub downloads a
# libmpv whose FFmpeg has no `pgssub` decoder, so PGS/HDMV subtitles fail with
# "Could not find subtitle decoder for format 'hdmv_pgs_subtitle'"
# (media-kit/media-kit#1269).
#
# The local copy is upstream 1.3.8 with four URLs changed to the `full` flavour
# from the same build pipeline — see the comment in its android/build.gradle.
# Re-sync it when media_kit bumps the package.
dependency_overrides:
  media_kit_libs_android_video:
    path: packages/media_kit_libs_android_video

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  drift_dev: ^2.34.0
  json_serializable: ^6.14.1
  flutter_launcher_icons: ^0.14.4
  http: ^1.6.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Generates AppLocalizations from lib/l10n/*.arb; see l10n.yaml.
  generate: true

  assets:
    # App mark, for in-app use (about screen, empty states, window icon).
    # Launcher icons themselves are generated into the native projects by
    # `dart run flutter_launcher_icons` and are not bundled from here.
    - assets/icon/icon.png
    - assets/icon/icon_256.png
    # Surfaced through LicenseRegistry so the font's OFL notice appears in the
    # About page's licences list. See main.dart.
    - assets/fonts/OFL.txt

  fonts:
    # Roboto is bundled rather than fetched at runtime (as `google_fonts`
    # would): the app must look right on first launch with no network, and
    # only Android supplies Roboto as a system font.
    #
    # This is the canonical variable font from google/fonts (OFL-1.1). It is
    # declared once with no `weight:` — since Flutter's font-weight-variation
    # change, a TextStyle's FontWeight drives the `wght` axis directly, so
    # every weight the design uses comes out of this one file.
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Variable.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
